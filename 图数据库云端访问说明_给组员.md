# 图数据库云端访问说明（给组员）

## 一、这是什么

中阿文旅文献知识图谱（SKWM 图数据库）已经搬到云端（Neo4j Aura 免费版），
**任何人、任何电脑、任何地方，只要能上网就能查**，不用装任何软件。

数据规模：
- 论文 11602 篇、作者 15159 位、主题 1110 个、期刊 4224 本、年份 91 年、领域 16 个
- 关系 105191 条（论文-主题、论文-作者、论文-期刊、主题共现等）

## 二、连接信息（重要，请保存）

| 项目 | 值 |
|---|---|
| 连接地址 (URI) | `neo4j+s://1c9ec2e0.databases.neo4j.io` |
| 用户名 | `neo4j` |
| 密码 | （找项目负责人要，不写在这） |
| 数据库名 | `neo4j` |

⚠️ 密码不要外传！这是团队内部资源。

## 三、怎么查（最简单的方式：网页）

**不用装任何软件，用浏览器就行：**

1. 打开：`https://console.neo4j.io`
2. 用团队账号登录（或找负责人要账号）
3. 点开实例 → 点"查询"（Open with Neo4j Browser）
4. 里面直接输入查询语句，回车就能看图

## 四、常用查询（复制即用）

**查全部数据量：**
```cypher
MATCH (n) RETURN count(n)
```

**查某篇论文连了什么：**
```cypher
MATCH (p:Paper)-[:HAS_TOPIC]->(t:Topic)
RETURN p.title, collect(t.name) AS topics
LIMIT 10
```

**查某个主题相关的论文（比如"旅游"）：**
```cypher
MATCH (p:Paper)-[:HAS_TOPIC]->(t:Topic)
WHERE t.name CONTAINS 'Tourism'
RETURN p.title, p.year
LIMIT 20
```

**查主题之间的最强共现（研究热点）：**
```cypher
MATCH (a:Topic)-[r:CO_OCCURS_WITH]->(b:Topic)
RETURN a.name, b.name, r.weight
ORDER BY r.weight DESC
LIMIT 20
```

**查某主题每年热度（时序）：**
```cypher
MATCH (t:Topic)-[s:SNAPSHOT]->(y:Year)
WHERE t.name = 'Cultural Heritage'
RETURN y.year, s.heat
ORDER BY y.year
```

**查作者写过哪些论文：**
```cypher
MATCH (a:Author)-[:AUTHORED]->(p:Paper)
WHERE a.name CONTAINS 'Ahmed'
RETURN a.name, p.title
LIMIT 10
```

## 五、用 Python 代码查（给要写代码的组员）

```python
from neo4j import GraphDatabase

URI = "neo4j+s://1c9ec2e0.databases.neo4j.io"
USER = "neo4j"
PASSWORD = "找负责人要"

driver = GraphDatabase.driver(URI, auth=(USER, PASSWORD))

with driver.session() as s:
    # 查主题共现热点
    rows = s.run("""
        MATCH (a:Topic)-[r:CO_OCCURS_WITH]->(b:Topic)
        RETURN a.name AS a, b.name AS b, r.weight AS w
        ORDER BY r.weight DESC LIMIT 10
    """)
    for row in rows:
        print(row["a"], "<->", row["b"], row["w"])

driver.close()
```

## 六、注意事项

1. 免费版有容量限制，**不要**跑超大查询（比如一次取全部 3 万节点），会慢/超时
2. 查询时加 `LIMIT` 控制数量，20-100 条就够看
3. 密码只在团队内部用，**不要**传到 GitHub 或公开地方
4. 数据更新：本地库改数据后，需要重新同步到云端（找负责人操作）
5. **云端连接偶尔会闪断**：免费版在网络波动时可能报"连接被重置/超时"，这不是数据问题，**等几秒重试一次**就行
6. **查关系类型时**：云端目前有 7 种关系（论文-主题、论文-作者、论文-期刊、论文-年份、主题-领域、主题共现、主题年度热度 SNAPSHOT），如果查询报"关系类型不存在"，先确认是不是拼写错了
7. **数据现状（2026-08 已验证）**：节点 32202（论文11602/作者15159/主题1110/期刊4224/年份91/领域16），关系 105191 条，与本地库完全一致
