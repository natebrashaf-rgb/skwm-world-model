# Domain 词表证据（2026-08-27，更正版）

## 更正（2026-08-27）：图库单字/符号 Domain 的来源
上一轮审计曾判断「Domain 单字/符号名源自 core_terms 词表 domain 字段既有质量问题」——**该判断有误**。
真相：修复前 11 条非阿语文献的 domains 被错误序列化成字符串（如 '["阿拉伯文旅"]'），
rebuild 用 set(字符串) 将其拆成单字符集 → 图库 Domain 节点出现 '"'、'['、']'、'阿'、'拉'、'伯'、'文'、'旅' 等单字/符号。
这与 terms 碎片同源（同一序列化 bug），不是词表质量问题。

## 证据
- 修复前（backup/topic_assignments.bak_20260826_184708.json, SHA b2060ae7）：domains 为字符串的条目数 = 24，
  字符串 domains 拆出的字符集 = ['"', '[', ']', '伯', '拉', '文', '旅', '语', '阿']
- 修复后（data/topic_assignments.json, SHA 3332966f）：domains 全部为正常 list，共 18 种，无单字/符号：
  ['中阿关系', '可持续发展', '地理', '政治', '政策法规', '教育', '数字技术', '文化', '文化艺术', '文化遗产', '文旅交叉', '旅游', '研究方法', '组织', '经济', '经济贸易', '阿拉伯文化', '阿拉伯文旅']
- core_terms.json（SHA 63dac6b6…，8/18 后未变）domain 字段本身无单字/符号问题：18 种
  说明：图库 Domain 节点来自 topic_assignments 的 domains 字段，不是 core_terms 的 domain 字段。
- 结论：文件层已修复（18 种干净 Domain），图库（19:01 重建版）仍为修复前含单字版本，再重建即消失。